# ── Cargo aliases for DPronix development ────────────────────────
#
# Usage: cargo <alias>   (e.g. `cargo check-all`)

[alias]
check-all   = "check --workspace --exclude dpronix-desktop --all-targets"
test-all    = "test --workspace --exclude dpronix-desktop"
clippy-all  = "clippy --workspace --exclude dpronix-desktop --all-targets -- -D warnings"
doc-all     = "doc --workspace --exclude dpronix-desktop --no-deps --document-private-items"
quickstart  = "run --example quickstart -p dpronix-cli"
audit       = "deny --all-features check"
coverage    = "llvm-cov --workspace --exclude dpronix-desktop"
